# Spacing

Use the shared scale for layout, with `space-5` (16px) as the default control rhythm. Dense admin views may tighten spacing; reading views may expand it. Do not introduce one-off values without a documented reason.
