# Accessibility

Target WCAG 2.2 AA. Preserve semantic headings, labelled controls, visible focus, keyboard navigation, screen-reader announcements, contrast and comfortable touch targets (44×44 CSS px minimum where practical). Never use color alone to communicate state.
