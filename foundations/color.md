# Color

Use semantic roles from `tokens/color.json`, never raw palette names in product contracts. Light and dark themes are composed independently; dark mode is not an inversion.


Accent is reserved for selection, progress, focus and primary actions. Status colors must also have text, icon or structural support and meet WCAG 2.2 AA contrast.
