# Iconography

Web/Desktop may use Lucide with consistent stroke, size and alignment. Icon-only actions require an accessible label; add a tooltip on desktop when the meaning is not obvious. Android should choose a platform-native equivalent with the same semantic meaning.
