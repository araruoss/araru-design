# Motion

Motion explains state change, spatial continuity or feedback. Use the shared durations and always provide a reduced-motion path (`prefers-reduced-motion` on Web/Desktop and platform equivalent on Android).
