# Typography

Use the small scale in `tokens/typography.json`. Sans is for navigation and controls; the reading face is optional for long-form metadata or reader surfaces. Prefer readable line length and `reading` line height.
