# Web

Implement tokens as CSS custom properties and semantic HTML. React/TypeScript, Base UI, shadcn/ui and Tailwind are implementation choices, not design contracts. Test keyboard, screen readers, responsive reflow and `prefers-reduced-motion`.
