# Offline and reconnecting

Show offline and reconnecting as persistent, actionable state. Preserve local reading position where possible, communicate sync status and never imply a write was saved when it was not.
