# Tabs

Use for sibling views at the same hierarchy. The active tab must be announced and keyboard navigation must follow the platform convention. Persist the selected view in URL state when it represents a meaningful administrative sub-route, so refresh and deep links preserve context. On compact layouts, reflow tabs before allowing primary content or labels to be clipped.
