# Reader dock

Contains only prioritized previous, next, position/progress and optional table-of-contents actions. It adapts between desktop, tablet and mobile; it never relies on hover or tiny icon-only targets.
