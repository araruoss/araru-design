# Settings field

Anatomy: label, description, control, validation/error and optional restart-required or dangerous-action notice. Selectors and text fields should use content-sized maximum widths when the value does not require a full form column; preserve a wider control only for values such as installation identity or long IANA/provider identifiers. Group related settings into a stable section and expose saving, saved, reset, read-only and conflict states.
