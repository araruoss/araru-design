# Components

These are framework-independent behavioral contracts, not implementation code. Each component must define purpose, anatomy, states, keyboard behavior and accessible naming before a client implements it.
