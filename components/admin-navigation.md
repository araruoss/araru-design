# Admin navigation

Separate navigation context for operational destinations. Expanded screens use a sidebar; compact screens use a drawer or stack. The active destination and return path are announced.
