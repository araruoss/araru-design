# Library grid

Provides a fluid cover-led collection whose columns respond to available width and minimum readable item size. It supports loading, empty, error, selection, focus and virtualization without requiring all items to mount.
