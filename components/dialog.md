# Dialog

Use for focused decisions or destructive confirmation. Trap focus, label the dialog, close with Escape where safe, and return focus to the trigger. Forms with multiple fields may scroll inside a bounded dialog, keep the submit action visible, and expose validation near the relevant field. Destructive dialogs state the consequence and offer an explicit cancel action; they must not rely on browser-native overflow or clipped controls.
