# Reader shell

Owns the immersive reading surface, safe-area handling, format engine boundary and reader lifecycle. It must provide an accessible exit and preserve the last known position when leaving.
