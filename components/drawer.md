# Drawer

Use for contextual navigation or secondary content on compact screens. Preserve a clear close action and avoid hiding primary reading content unnecessarily.

When opened, move focus into the drawer; when closed, return focus to its trigger. Escape closes it when no safer nested action is active. The drawer must expose the same actions through keyboard and touch.
